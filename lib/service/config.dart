class HttpConfig {
  static const String baseUrl = 'https://www.freebuf.com/fapi/frontend/';
  static const Duration timeout = Duration(seconds: 5);
}

class RequestConfig {
  static const int COUNT = 20;
}
